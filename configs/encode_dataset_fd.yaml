dataloader:
  _target_: torch.utils.data.DataLoader
  batch_size: 64
  num_workers: 8
  shuffle: true

defaults:
  - data: finedance
  - defaults
  - _self_

run_dir: ???

ckpt_name: last
device: cuda
